apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  addresses:
    -  10.0.0.210-10.0.0.250
